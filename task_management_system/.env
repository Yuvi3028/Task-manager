# Database configuration
DB_NAME=db
DB_USER="your Username"
DB_PASSWORD="Your Password"
DB_HOST=localhost
DB_PORT=3306


# Django secret key (it’s best to generate this securely via `secrets.token_urlsafe(50)`)
SECRET_KEY= "Put your Django Secret Key"

# Optional: Environment (e.g. to distinguish between development and production)
DJANGO_ENV=production

# Add any other necessary configurations
